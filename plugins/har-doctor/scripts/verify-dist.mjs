import { access, readFile } from 'node:fs/promises';import path from 'node:path';import{fileURLToPath}from'node:url';const root=path.dirname(path.dirname(fileURLToPath(import.meta.url))),d=path.join(root,'dist');for(const f of ['plugin.json','main/index.html','preload/index.cjs','logo.svg'])await access(path.join(d,f));const m=JSON.parse(await readFile(path.join(d,'plugin.json')));if(m.development||m.main!=='main/index.html')throw new Error('Invalid dist manifest');console.log('har-doctor dist verified');
