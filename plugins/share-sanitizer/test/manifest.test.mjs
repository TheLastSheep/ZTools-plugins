import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
test('manifest feature contract is narrow and preload bridge has no path exposure',async()=>{const manifest=JSON.parse(await readFile(path.join(root,'plugin.json'),'utf8')),preload=await readFile(path.join(root,'preload','index.cjs'),'utf8');assert.deepEqual(manifest.categories,['productivity','text','media']);const cmds=manifest.features[0].cmds;assert.equal(cmds.find((item)=>item?.type==='files').maxLength,1);assert.ok(cmds.find((item)=>item?.type==='img'));assert.ok(cmds.find((item)=>item?.type==='over'));assert.ok(!preload.includes('getPathForFile'));});
test('renderer clears dynamic findings without HTML injection and labels metadata-only export accurately',async()=>{const app=await readFile(path.join(root,'src','ui','app.mjs'),'utf8'),html=await readFile(path.join(root,'src','ui','index.html'),'utf8');assert.ok(!app.includes('innerHTML'));assert.match(html,/Export re-encoded image/);assert.doesNotMatch(html,/Copy cleaned image/);});
