import { access,readFile } from 'node:fs/promises';import path from 'node:path';import {fileURLToPath}from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),d=path.join(root,'dist'),m=JSON.parse(await readFile(path.join(d,'plugin.json'),'utf8'));if(m.development)throw Error('development leaked');for(const f of [m.main,m.logo,m.preload,'core/guard.mjs'])await access(path.join(d,f));if(/node:|\.\.\/core\//.test(await readFile(path.join(d,'app.mjs'),'utf8')))throw Error('renderer imports Node code');console.log('plugin-guard dist verified');
