import{access,readFile}from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),d=path.join(root,'dist'),m=JSON.parse(await readFile(path.join(d,'plugin.json'),'utf8'));if(m.development)throw Error('development leaked');for(const f of [m.main,m.logo,m.preload,'core/archive.mjs'])await access(path.join(d,f));if((await readFile(path.join(d,'app.mjs'),'utf8')).includes("../core/"))throw Error('dist UI import escaped package');console.log('archive-workbench dist verified');
